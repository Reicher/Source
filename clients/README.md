# Source clients

Platform clients live under this directory and share the versioned contracts in
`../contracts`. Each client remains independently buildable; adding a desktop or
iOS client later does not require nesting it inside the Android build.

- [`android`](android/README.md) — the first official Source Client.
